"""
Diseña una función llamada area_triangulo(base, altura) que calcule el área mediante (base * altura) / 2 y valide que ambos valores sean mayores a cero.

def area_triangulo(base, altura):
    if base > 0 and altura > 0:
        return (base * altura) / 2
    else:
        return 0

print(area_triangulo(10, 5))
print(area_triangulo(7, 4))
print(area_triangulo(-2, 5))
"""
